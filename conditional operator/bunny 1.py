# Формат ввода
# Строка описывающая придорожную местность.

# Формат вывода
# YES — если в этой местности есть зайка, иначе — NO.

text = input()
if "зайка" in text:
    print("YES")
else:
    print("NO")
